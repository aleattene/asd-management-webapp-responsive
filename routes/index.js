
// HOMEPAGE ============================================================================================================
export default async function home(fastify, options) {
    fastify.get('/', async (req, reply) => {
        return { hello: 'homepage' }
        }
    )
}
//======================================================================================================================